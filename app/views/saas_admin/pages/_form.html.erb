<%= f.error_messages %>

<p>
  <%= link_to "Formatting guide", "http://daringfireball.net/projects/markdown/syntax", :target => "_blank" %>
</p>

<div class="field string">
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>

<div class="field text">
  <%= f.label :body %>
  <%= f.text_area :body %>
</div>

<% unless f.object.new_record? %>
  <!--
  <div class="field string">
    <%= f.label :slug %>
    <%= f.text_field :slug %>
  </div>
  -->
<% end %>

<%= submit_or_cancel %>
